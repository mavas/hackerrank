/**
 * https://codeforces.com/gym/101736/problem/G
 */


#include <stdio.h>


int main(int argc, char* argv[])
{
    int count = 0;

    int x1, y1, x2, y2;

    //while (count < 600)
    while (count < 1)
    {
        char c;

        printf("Q %d %d %d %d\n", x1, y1, x2, y2);
        scanf("%c\n", &c);
        //printf("\t%c\n", c);

        count += 1;
    }

    return 0;
}
